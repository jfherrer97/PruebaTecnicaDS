<%= form_with(model: cryptocoin) do |form| %>
  <% if cryptocoin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cryptocoin.errors.count, "error") %> prohibited this cryptocoin from being saved:</h2>

      <ul>
        <% cryptocoin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :monthly_return, style: "display: block" %>
    <%= form.text_field :monthly_return %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
